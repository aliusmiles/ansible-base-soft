---
# tasks file for ansible-base-soft
- name: Include OS-specific variables
  include_vars: "{{ ansible_os_family }}.yml"

- name: Include OS-specific install tasks
  include: "install-{{ ansible_os_family }}.yml"
